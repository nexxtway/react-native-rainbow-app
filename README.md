### ⌨️ Running in iOS simulator

1. git clone https://github.com/nexxtway/react-native-rainbow-app.git
2. cd nalamapp
3. yarn install
4. cd ios && pod install
5. open ios project in Xcode
6. build and run
